# 基础关卡规则（Backend 之间必须逐位一致）

这份文档是唯一权威。`sim/torch_sim.py`（参考实现）和 `sim/cuda/*.cu`（高速实现）
都必须严格按这里的顺序和判定执行，`tests/test_parity.py` 会逐 tick 比对两者状态。

## 地图与角色

- `H x W` 网格，默认 13x13。格子只有两种：地板、**不可破坏**墙体。
- 没有道具、没有可炸碎的墙体、没有踢泡泡。速度/威力/泡泡数全部固定。
- `P` 个角色（2..4），初始位置在四角格中心（`x + 0.5`），四角及其相邻格保证为地板。
- 每个角色属性固定：`MAX_BOMBS` 个同时在场泡泡，爆炸半径 `BLAST`，
  移动速度 `SPEED` 格/秒。

## 时间与坐标

- 逻辑帧率 `TICK_HZ = 15`（**不是**渲染帧率）。每 tick 是一次决策。
- 坐标是 **float32 连续量**，`pos` 记角色中心，格 `(r, c)` 的中心是 `(r+0.5, c+0.5)`。
- 一个 tick 沿当前方向匀速位移 `STEP_LEN = SPEED / TICK_HZ`
  （默认 3/10 = 0.3 格）。没有惯性、没有加速度：按下即走，松手即停。
- 碰撞盒是轴对齐正方形，半宽 `RADIUS`（默认 0.3），约束 `0 < RADIUS < 0.5`，
  并要求 `STEP_LEN < 1 - 2*RADIUS`（`sim/config.py` 里有断言）。这两条一起保证：
  碰撞盒最多同时压在 2 行 2 列上，单轴移动只需检查前方 **2 个格子**，
  且单 tick 位移不可能穿过一格 —— 因此**不需要 substep**。

## 动作空间（因子化，两个头）

不是一个 6 选 1 的扁平空间，而是两个独立的头：

| 头 | 取值 | 含义 |
| --- | --- | --- |
| move | `0=上 1=下 2=左 3=右 4=IDLE` | 本 tick 持续朝该方向匀速走；IDLE 原地不动 |
| bomb | `0=不放 1=放` | trigger，与移动**同帧并行** |

炸弹人的核心操作是"放泡的同时继续跑"。扁平的 6 动作会强迫放泡那一帧站着不动，
是错的。因子化之后联合 `log_prob` = 两头之和、熵 = 两头之和，PPO 的 `ratio`
仍是单个标量，公式一行都不用改。

## 一个 tick 的执行顺序

严格按此顺序，不可交换：

1. **引信递减**：所有 `fuse > 0` 的格子 `fuse -= 1`。
2. **放泡**：按角色编号 `0..P-1` 升序处理。角色存活、`bomb == 1`、
   **移动前**的中心格无泡泡、且自己在场泡泡数 `< MAX_BOMBS` 时，在该格写入
   `fuse = FUSE`、`owner = 角色编号`。（本 tick 新放的泡泡不会在本 tick 爆炸。）
3. **移动**：`blocked = 墙 | 有泡泡`。存活且 `move != IDLE` 的角色，
   按 `dy, dx = DIRS[move] * STEP_LEN` 位移，**逐轴**消解碰撞：
   - 前缘所在行/列 = `floor(coord ± RADIUS)`；另一轴横跨的两格 = `floor(other ∓ RADIUS)`。
   - 任一格不可通行 → **贴着障碍物停下**（滑动），而不是整步作废：
     正向停在 `lead - RADIUS - EPS`，负向停在 `lead + 1 + RADIUS + EPS`，`EPS = 1e-4`。
   - 两个轴都用**移动前**的坐标做判定，顺序无关。
   - **脚下刚放的泡泡必须能走出去**：判定方式是"碰撞盒当前是否已经压在这一格上"
     —— 压着就放行，走出去之后它自然变回实体。无状态，两个 backend 都好实现。
   - **角色之间不碰撞**（可互相穿过，原作行为）。这顺手删掉了格子版那段 O(P²)
     的换位/同格消解，是移动阶段唯一的串行块。
4. **爆炸与连锁**：`fuse == 0` 且有 owner 的格子成为爆源。
   爆炸范围是十字射线：从爆源向四个方向逐格推进，最多 `BLAST` 格，
   **遇到墙体立即停止（墙体本身不受影响）**；**泡泡也挡火** —— 火焰覆盖
   泡泡所在的格（把它点燃），但不穿过它继续延伸（经典炸弹人规则，放泡
   可以当屏障）。连锁：被火焰覆盖到的、仍有引信的泡泡立刻也成为爆源，
   下一轮从它自己向外扩散（不靠穿透传播），最多 `MAX_CHAIN` 轮
   （默认 8，达到上限则截断，属已知近似）。
5. **判定伤害**：所有存活角色，若**中心格**（`floor(pos)`）在本 tick 火焰范围内则扣 1 血
   （`hp = max(0, hp-1)`），血归 0 才死亡。命中盒故意比碰撞盒小 —— 否则贴着墙走会被
   隔着墙的火焰蹭死，手感说不通。同一 tick 内同时判定，不存在先后。放泡者不豁免——
   会被自己的泡泡炸死。`max_hp` 默认 5；`max_hp=1` 等价旧版"一碰就死"。
   血量**不进观测**（布局仍是 2P+3），它只存在于模拟器状态和 UI 血条。
6. **清场**：本 tick 爆炸的格子 `fuse = 0`、`owner = -1`，泡泡额度归还。
7. **计步与终局**：`t += 1`。`存活人数 <= 1` 或 `t >= MAX_STEPS` 时 `done = True`。

## 奖励

**分数分两个层面，互不混用**：

- **微观分（每 step 塑形）**：只进 rollout buffer 做策略梯度（"局内学习"），
  局终丢弃，**不累计进 ELO**。
- **宏观分（终局一次性）**：局终胜负折算成一条 terminal reward 也进梯度 ——
  否则策略收不到"要赢"的信号，只会龟缩角落（实测的坍塌行为）。
  ELO 只由胜负统计驱动（标准 ELO，`expected = 1/(1+10^((opp-self)/400))`，
  `delta = k×(actual-expected)`），与上述任何分数完全独立。

微观（每 tick）：

- `-step_penalty`（默认 0.001）。
- **危险区站桩罚**：站在被在场泡泡爆炸范围覆盖的格，每 tick 扣
  `-danger_penalty × danger值`（danger 值 = `1-(fuse-1)/FUSE`，越接近爆炸越疼）。
  这是"学会躲开自己放炮"的直接梯度 —— 和观测危险通道同源，网络有直接监督。
- **久不放炮罚**：连续 `passivity_ticks`（默认 60，4 秒）tick 没放泡后，每 tick
  `-passivity_penalty`。**有泡泡预算（还能放）才扣**；放满（所有炮都在场）不扣，
  只有"消极摆烂"被罚，"积极进攻"不被冤枉。
- 掉 1 血：`-hit_reward`（默认 1.0）；1v1 里对方掉血 = 我方造成伤害 `+hit_reward`。

宏观（终局一次性，默认 `win_hp_scaled=True` **按剩余血量给**）：

- **胜利 `+win_bonus/max_hp × 剩余血量差`**（自己剩余血 − 对手平均剩余血），
  失败为负、平局 `0`。**满血差击杀拿满 `win_bonus`，残血险胜按比例少拿**（如
  剩 1 血险胜 = `win_bonus/max_hp`）—— 反"拿血换命"的 reward-hacking：旧版
  离散 `±win_bonus` 只看谁活着，领先 1 血时 AI 会练出"残血换命照样 +8"。
  双亡（血量全 0）→ 差 0 = 平局。
- **超时（全员存活）判血**：血多者胜 `+win_bonus/max_hp × 血量差`、血少者输、
  血平 `0`（血量差给分同样适用于超时）。
- 同时死光：平局 `0`。
- `win_hp_scaled=False` 时回退旧离散规则：胜利 `+win_bonus` / 失败 `-win_bonus` /
  平局 `0`（只看胜负不看血量）。
- 注意：**胜/平/负布尔（info['winner'] 与 ELO）始终按离散胜负算**，只有 reward
  数值按血量缩放 —— ELO 不受影响。

> 击杀归属（"是我的泡泡炸死了谁"）v1 没有实现：1v1 里对方掉血必然来自我方的
> 火焰，不需要归属追踪；做 1vN 击杀奖励时再补，见 `sim/blast.py` 里的 TODO。

## 观测张量

**一个 env 只存一份**，形状 `(N, 2P+3, H, W)`，dtype 默认 **fp16**
（`cfg.obs_fp16`）。通道数 `C = 2P + 3`：2 人 = 7、3 人 = 9、4 人 = 11。

存储序（这是落在显存里的顺序，两个 backend 必须一致）：

| 通道 | 内容 |
| --- | --- |
| 0 .. P-1 | 玩家 i 的位置（**双线性 splat**，权重和为 1；死亡则全 0） |
| P .. 2P-1 | 玩家 i **名下**泡泡的引信，`fuse / FUSE` |
| 2P | 墙体，0/1（整局不变） |
| 2P+1 | 危险度：被火焰覆盖的格子取 `1 - (fuse-1)/FUSE`，多泡泡取最大值 |
| 2P+2 | 步数进度，常量平面 `t / MAX_STEPS` |

所有通道都**与"我是谁"无关**，所以不需要每个角色存一份。角色 `me` 的视角
只是这份张量的一个**纯置换**：

```
view[0]        = 存储[me]          自己位置
view[1]        = 存储[P + me]      自己泡泡引信
view[2..P]     = 存储[other]       各对手位置（编号升序）
view[P+1..2P-1]= 存储[P + other]   各对手泡泡引信
view[2P..2P+2] = 存储[2P..2P+2]    墙 / 危险 / 进度
```

由 `sim.config.view_perm(me, P)` 给出。**置换输入通道等价于置换第一层卷积
权重的输入通道维**，所以训练侧一个字节都不用搬（推导见 `train/model.py`，
等价性由 `tests/test_train.py::test_weight_perm_equals_data_gather` 钉住）。
泡泡引信按 owner 分通道、不合并成一条"对手泡泡"：合并要求和，一旦求和，
视角就不再是置换，这个技巧立刻失效；分开存还顺手把泡泡归属信息喂给了网络。

这一步换来的是 P 倍的写入量：13x13 / 4 人下从 24,336 B/env-tick 降到
3,718 B/env-tick（含 fp32→fp16），`observe` 的纸面上限从 41 M 抬到 271 M
env-steps/s —— observe 本来是整个模拟器的第一瓶颈。

位置通道用**双线性 splat** 而不是 one-hot：一个 tick 只走 0.3 格，one-hot 会把
这 0.3 格的位移量化掉，网络看到的是"连走 3 帧才动一下"。splat 把亚格偏移
保留在相邻 4 格的权重里。**两个 backend 的四次加法顺序必须一致**，
否则 float 求和的最后一位会不同。中间量一律 fp32 计算，只在写出那一刻 cast
成 fp16 —— 两边都这么做，parity 才有意义（容差按 fp16 分辨率给，约 1e-3）。

危险度通道是"放泡瞬间就把整个时空影响范围画出来"的那张图：它对**当前
在场的所有泡泡**做一次射线展开，越接近爆炸值越接近 1。网络因此不需要自己
从坐标推断威胁。

## 合法动作掩码

两个头各自一张掩码，都作用在 **logits 上加 `-inf`**，绝不用乘概率的方式。

- `move_mask` (N,P,5)：只屏蔽"按了也一格都动不了"的方向 —— 逐方向真跑一遍
  第 3 步的碰撞消解，位移小于 `2*EPS` 才算屏蔽。`MOVE_IDLE` 恒合法。
- `bomb_mask` (N,P,2)：泡泡数用尽或脚下已有泡泡 → 屏蔽 `bomb=1`。`bomb=0` 恒合法。

因为每个头都至少有一个恒合法动作，**整行全屏蔽不可能发生**，格子版那个
"全被屏蔽就把整行放开"的 NaN 兜底分支因此彻底删掉了。死亡角色的动作不会被
执行，两个头都整行放开，省掉调用方的特殊分支。

> 注意 `RADIUS < 0.5` 意味着角色在一个封闭格里还有 `0.5 - RADIUS` 的余量
> （默认 0.3 格）。所以"四面是墙"时朝墙的方向通常**仍然合法**（能贴过去），
> 只有已经贴住之后那一个方向才会被屏蔽。
> `tests/test_rules.py::test_boxed_in_player_still_has_idle_and_no_bomb`
> 固定了这个行为。
