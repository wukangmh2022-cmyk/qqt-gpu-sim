# QQT GPU Sim 待办事项与问题跟踪 (todo.md)

## 1. 待解 Bug 跟踪

### Bug 1: 11debug 三面受困向障碍挤压死锁震荡 (暂存)
- **发现场景**: 
  - 录像 `11debug` 中，AI（P0）在三面受困（如障碍/泡泡邻格贴近）情形下，角色距离障碍格仅剩约 0.25 格微小缝隙。
- **现象细节**:
  - 角色试图向左走，但左边已被水泡封堵。角色在该处开始高频上下抖动、滑步挤压，直至被水泡爆炸击杀。
- **机理与根因**:
  1. **微距物理掩码过度放行**: `legal_mask` 基于物理微距探针，检测到角色当前坐标与障碍物边界之间尚有 0.25 格的连续物理余隙，因而判定“向左”为合法动作（掩码为 1）。
  2. **宏观目标牵引与微观执行撕裂**: 全局价值网络与特征在宏观地图上看到左方是通往敌方/道具的通路，持续输出“向左”动作。
  3. **`_steer` 丢弃微步并转向侧滑**: `_steer` 尝试向左移动，但由于 0.25 格不足以完成跨格步进（未走满），且无法穿透障碍物，直行位移被丢弃；`_steer` 随后尝试垂直两侧滑移归中，导致角色在两堵墙/死胡同垂直方向反复颠簸震荡，卡死在障碍面前。
- **备选解决方案**:
  - **方案 A (离散掩码安全网 / Macro-Grid Safety Net)**: 
    - 当目标格在离散语义上是不可穿透的物理障碍（墙体、已放置水泡）时，即便微观上有 <0.5 格微隙，除非处于明确的半身位避让场景，否则禁止将向障碍前进标为合法动作，防止模型向死路挤压。
  - **方案 B (物理微步 + 水泡硬阻断 / Hard Bomb Barrier)**:
    - 严格区分“静态走廊墙体”与“致死水泡”：在 `legal_mask` 中，若目标格存在已放置水泡，且角色没有越过分界线，直接硬屏蔽朝向水泡的动作；允许反向退步。
  - **方案 C (引入时间感知死胡同预判 / Spatiotemporal Trap Invariance)**:
    - 借鉴时空规则 AI 的 `would_be_trapped` 机制：在安全掩码/特征层预判“进入该格或持续朝向该障碍走是否会导致安全出口归零”，若死胡同无法逃逸则强制在动作掩码中剔除或施加强力惩罚。

---

## 2. 模拟器与规则对齐 (已排期/进行中)

- [x] **Bug 2: 22debug 开阔地向左走不过去修复**: 已修复 `targetOpen && enteredTarget` 判定与进门侧滑中线截断 (Commit: `735ee8b`)。
- [x] **爆炸余威对齐**: 
  - 统一为 `FLAME_LINGER_MS = 250ms` (0.25s / 2 ticks @ 10Hz)，保证 Python/JAX/JS 绝对一致 (Commit: `5252ef0`)。
- [ ] **强化学习新特征 (Feat) 调研与引入**:
  - 基于高级时空危险窗与 KataGo 势场思想，设计死胡同逃生余量场（Escape Margin Map）、势场控制力（Territory Influence）、连续危险到达时间（Time-to-Danger）通道。
