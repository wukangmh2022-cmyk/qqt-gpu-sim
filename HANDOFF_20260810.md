# DCU 训练完整交接上下文（2026-08-10，全量版）

> 给新 session 的完整交接文档。覆盖：项目全貌、全部历史改动（含本会话前）、
> 奖励体系演化、修复的 bug 清单、性能优化与 profile 数据、分析结果、测试、已知坑、待办。
> 接手先读本文件，再按需读代码。

## 1. 项目一句话

qqt-gpu-sim = Bomberman 风格 1v1 对战，GPU 批量模拟器（BatchedSim 纯 tensor）+ PPO self-play。
训练在远程 DCU（海光 HIP/ROCm）服务器，本机做开发/启动器/分析。目标：课程化 self-play 学人类打法，
参考 arXiv 2407.00662（Pommerman Curriculum + Population-based Self-Play，无开源权重）。

## 2. 服务器凭据与连接

- SSH：`root@ssh.zzai.scnet.cn` 端口 `10630`，密码 `YOUR_PASSWORD（DCU_PASS，见根目录 .env）`（`JQ4FT` 不是 `JQ4F`）
- Banner 握手慢：必须 `-o ConnectTimeout=30`，失败重试；本机有 sshpass
  `sshpass -p 'PASS' ssh -o ConnectTimeout=30 -o StrictHostKeyChecking=no root@... -p 10630`
- DCU：`source /opt/dtk-26.04/env.sh`，torch 2.9.0 HIP（cuda=True），Python 3.11.9，单卡，128 CPU 核
- 代码在 `/root`（不是 /root/qqt-gpu-sim）；本机 `/Users/pippo/operater-dev/qqt-gpu-sim`
- 服务器离线（pip 不可达）；pytest 9.1.1 已离线装好
- GPU 利用率 HCU% 平时应 >50%；launch-bound 时会低（这是已知瓶颈特征）

## 3. 当前训练状态（2026-08-10 07:31 重启后）

- **PID 9952**，resume `private_data/duel_course.pt`（1.0439B 快照），现 ~1.05B 步
- sps ~12-15k（本会话优化前 10-11k，+27%）
- 命令 = `/root/relaunch_1023m.sh`（见第 4 节），日志 `/root/train_1023m_finetune.log`，
  CSV `/root/ckpt/train_1023m_finetune.csv`（append，时间线连续）
- 快照每 20 迭代写 `private_data/duel_course.pt`；崩溃 finally 也存盘
- CSV 列：step,stage,elo,win_rate,ep_len,pg,vf,ent,kl,clipfrac,sps,alpha,suicide_rate,bombs_per_ep,danger_frac,bc

## 4. 当前训练命令（relaunch_1023m.sh 等价）

```
cd /root && source /opt/dtk-26.04/env.sh
nohup python -m train.train --backend torch --device cuda --arch mlp --single-stage \
  --map-mode corridor --open-fraction 0.5 --ring-fraction 0 --hazard-fraction 0 \
  --num-envs 5632 --total-steps 1_200_000_000 --rollout-steps 128 --minibatches 4 \
  --warmup-steps 150_000_000 --fixed-opp-prob 0.4 --bot-opponents greedy,astar,hunter \
  --fixed-bots astar,hunter --explore-anneal \
  --bc-data recordings/ --bc-coef 0.3 --bc-batch 256 --bc-every 1 \
  --fixed-ckpt rw8=private_data/duel_rw8.pt --fixed-ckpt 5x2=private_data/duel_5x2.pt \
  --fixed-ckpt 5x3=private_data/duel_5x3.pt --fixed-ckpt cnn=private_data/duel_cnn.pt \
  --snapshot-every 20 --time-budget 43200 --seed 0 --oversample-dying 3 \
  --resume private_data/duel_course.pt --ckpt private_data/duel_course.pt \
  --log-csv ckpt/train_1023m_finetune.csv --combo-reward 0.10 --timeout-draw --lr-final 1e-4
```

## 5. 架构与关键模块

- `sim/config.py`：SimConfig（全部默认值/奖励系数），`sim/torch_sim.py`：BatchedSim 纯张量模拟器
- `sim/blast.py`：danger_map（危险图）/ resolve_explosions / rays，波前链式传播
- `sim/bots.py`：规则对手 random/greedy/astar/hunter（BotWrapper，全向量化无逐 env 循环）
- `sim/move.py`：move_players（_resolve_axis 碰撞消解，训练/启动器共用）
- `sim/obs.py`：observe / legal_mask / can_place
- `train/model.py`：ActorCritic（MLP 345k / CNN，视角用权重通道重排，obs 共享不搬数据）
- `train/ppo.py`：SelfPlayRunner.collect（含健康度统计，零 host 同步）+ PPO 更新
- `train/train.py`：主循环/课程/对手池/BC 引导/CSV 指标
- `train/bc.py`：人类录像 BC（69 局 3.9 万步）；`train/curriculum.py`：课程与 ModelPool
- `play/launcher.py`：图形启动器（危险图显示/音效/人类微操）
- `scripts/`：分析/工具脚本（见第 9 节）

观测 14 通道 (2P+3 视角置换 + extra)，obs_fp16=True，13×13 地图。视角 = 第一层权重按 pid 重排列块。

## 6. 奖励体系（当前默认 + 演化）

**当前值**（sim/config.py 默认，训练直接生效）：
- 稀疏：win_bonus=10.0（击杀固定 ±10，win_hp_scaled=False）、hit_reward=1.5、timeout_draw=True（超时生命差×退火 α）
- 稠密：danger_penalty=0.015、brick_reward=0.05、place_cover_reward=0.05、place_chain_reward=0.20、combo_reward=0.10
- 已删除（值为 0）：suicide 罚、approach_reward、passivity_penalty、place_dist_reward、chain_blast_bonus、chase
- 探索退火：α=1-tanh(1.2·2·击杀率)，explore_coef 随击杀能力归零；熵 entropy_coef=0.05 / entropy_final=0.03；lr=1e-4
- 其他：max_bombs=10、growth_bombs_max=10、max_chain=16、speed=3.0、max_steps=1800、open_fraction=0.5

**演化历史**（理解为什么这样设）：
1. 原方案有 suicide 重罚 → 用户要求删（自杀已由 danger_penalty + 真实胜负解决）
2. 终局按血量给分 → 改"对手 hp=0 才给，固定 ±10"（win_hp_scaled=False），防残血磨分
3. 平局 timeout → 改"胜者按生命差×退火 α"（timeout_draw=True），防龟缩且不激励残血自杀
4. 开启 combo_reward=0.10（连击，combo_gap_factor=0.9）；删除 approach/chase/passivity/place_dist/chain_blast_bonus
5. 训练日志新增 alpha/suicide_rate/bombs_per_ep/danger_frac/bc 指标，指导迭代

## 7. 修复的 Bug 清单（全部有测试/验证）

1. **危险图连锁 bug**（启动器显示 + 训练同源）：10 颗横向连泡必须先放的深、后放的浅（错误），
   实际同 tick 一起爆。修复：danger_map 波前链式传播（连锁组内危险值统一取组内最危险），
   训练端 observe 与启动器显示同源（共用 danger_map）→ 训练也给到正确危险度
2. **漏爆 bug**（用户实测）：max_chain=8 截断 ≥9 炸弹长链，本应被引爆的泡没爆。
   修复 max_chain=8→16（13×13 一行最多 13 泡需 12 轮连锁；early_exit 使额外轮免费）。测试固化
3. **钟摆效应**（确定性对称均衡零梯度）：entropy 退火到 0.002 → 确定性 + 对称对手 → 零梯度冻结。
   修复 entropy_final=0.03 + lr=1e-4。测试验证 3 seed 冻结 96→0
4. **astar 逃生 3 深 bug**：逃生评分被 Vopp=inf 污染（对手被泡阵困住）、fallback 选停、危险时禁停缺失
   → 只修 escape 用 Vsafe、fallback 优先非停、danger≥0.5 禁行 + 禁停。3 seed 冻结 96→0
5. **hunter 吃箱子层**（新功能 + 行为修正）：属性 ≥2 项 <70% 且场上有宝箱 → 高优先级寻路吃箱
   （V_crate Dijkstra 场 + dng>=0.5 禁行 + 禁停 + 可达性检查）；用户反馈"吃箱时变笨发呆"后修正
6. **hunter 吃箱子层广播 bug（本会话，训练崩溃根因）**：`eat_no_idle (N,) < eat[:, 4:5] (N,1)`
   广播成 (N,N) → expand_as 崩（5632 env 才炸，n=1 测试漏网）。
   修复 `unsqueeze(1)`；防回归测试 `test_hunter_eat_branch_large_batch_no_crash`（n=64）
7. **通行一致性**（训练 legal_mask vs 启动器 _player_frame_move）：像素级 4 方向全地图验证——一致
   （共用 _resolve_axis）。`scripts/passability_check.py` 保存为手动脚本（非常驻测试）
8. **启动器音效**：只播人类玩家位置的声音
9. **奖励因子分析 bug**：auto_reset 循环/term_win 每 tick 重复记录/brick 判定用 step 前位置——已修
10. **clear_stats 旧字典 KeyError 'suicide'** → 更新包含新键

## 8. 性能优化与 profile 数据

**之前的会话（训练端 + 启动器共同受益）**：
- danger 图缓存 dedup：step 算一次，observe 复用（_dng_cache + _dng_signature 用 torch._version），tick 成本 12.5→5.2ms
- 早退 + 空场短路（danger_map 无泡返回全 0）
- 4-way pad merge 尝试后回滚（CPU 上 -43%，因算子数增加），代码留注释警告勿用

**本会话（全部逐位一致验证）**：
1. **danger_map 阶段 A 双缓冲**（sim/blast.py）：波前权重+剩余距离一起挪格，pad ÷14（原按 blast 档分组 Σb，现统一 max_b）。`scripts/verify_danger_v2.py` 逐位一致 PASS
2. **astar/hunter 每 tick GPU→CPU 同步清除**（sim/bots.py）：
   - `_dijkstra_field` 收敛判定 `bool(torch.equal(dist, old))` 每 8 趟查（corridor 收敛 8-16 趟，8 最优，16 反慢）
   - 威胁膨胀轮数 `int(blast_cap.max().item())` → 静态 `cfg.growth_blast_max`
   - hunter `bool(crate.any())` → 向量化
   - `_mode_ticker` 每 16 tick 查（**必须 `%16==0 and bool(...)`，写 `or` 因短路每 tick 同步**）
3. **early_exit 降频**（sim/blast.py）：resolve/danger 每 CHECK_EVERY=2 轮查（结果逐位不变）
4. **放弃**：`blast_max_hint` 固定档位上限 = 负优化（空档 pad launch > max() 同步），已回滚留注释

**Profile 数据**：
- DCU（旧版 5632 env）：collect 98% + PPO 2%。collect 内：sim.step 44% / learner.act 38% / frozen-opp 9% / observe 8%
- **真实 11k vs profile 23.5k 差距 = astar/hunter bot 的 Dijkstra 场**（profile 用冻结网络当对手没测到）
- 本地 MPS 对照：加一个 astar bot，每 tick 57→100ms（bot 占 33-39%）
- 优化后服务器实测：sps 10-11k → 13-15k（+27%）
- **未挖**：learner.act 的 DCU 高开销（113ms/tick，345k MLP 5632 batch 异常）→ 需服务器空闲时 DCU kernel profile

## 9. 分析工具与结果

- `scripts/analyze_reward_factors.py`：71 局人类录像因子分析——因子分解/正交矩阵（独立性验证）/
  稀疏 vs 稠密分开求和。结论指导了奖励数值设定（如 hit=1.5 的密度匹配）
- `scripts/analyze_danger_timing.py`：danger 时间桶（U 形：≤0.5s 站立率 0.0091、0.5-1s 0.0078 最低、
  2.5-3s 0.0312 最高）→ 人类不站快爆格，危险图指数压缩 exp=2.0 依据
- `scripts/hourly_eval.py`：每小时评估（kill_rate/suicide_rate/dmg_taken/dmg_dealt/bombs_per_ep/danger_frac/ep_len + P1 swap）
- `scripts/passability_check.py`：训练/启动器通行一致性（手动）
- `scripts/prof_env.py`（本会话新）：独立 env profile，分段计时 + torch.profiler kernel 级，--opp astar/hunter/...
- `scripts/verify_danger_v2.py`（本会话新）：danger_map v2 逐位一致性
- `scripts/compare_ckpts.py` / `duel_arena.py` / `duel_swap.py` / `behavior_metrics.py` 等

**启动器危险图显示**：指数压缩（exp=2），同 tick 连锁组统一深度；训练共用同一 danger_map。

## 10. 测试（本地 119 passed + 7 skipped；服务器 89+ 关键测试）

- `tests/test_rules.py`：爆炸/连锁/长链（max_chain 16）/奖励/hazard
- `tests/test_bots.py`：逃生/吃箱子/危险禁停/hunter 大 batch 防回归/runner health/mode 切换
- `tests/test_train.py`：health stats、reward coefficients snapshot、BC
- 本会话新增：`test_hunter_eat_branch_large_batch_no_crash`、flaky 修复（mode 测试 200 tick + seed）
- 服务器跑测试：`source /opt/dtk-26.04/env.sh && cd /root && python3 -m pytest ...`
- **已知失败（忽略）**：sim/cuda parity（bomber_kernels.cu 编译问题，训练不用 cuda backend）；
  `test_res_assets_load_and_crop`（缺 pygame 资源库，环境问题）

## 11. 已知坑（务必记住）

- **`kill -INT` 对训练无效**：bash 交互式后台作业 SIGINT 被忽略（SigIgn 含 bit1）。SIGTERM 直接终止无 finally。
  优雅停 = 等快照点（20 迭代）后 SIGTERM → resume 最新快照，零丢失。gdb/py-spy 受 ptrace_scope=1 限制
- **lr resume 陷阱**：`opt.load_state_dict(ckpt["opt"])` 用 ckpt 旧 lr 覆盖 `--lr`，必须 `--lr-final 1e-4`
- **blast_max_hint 是负优化**（见上），勿重新启用
- **CSV append 模式**：重启复用时间线连续；resume 后第一行是垃圾行（ep_len=0）跳过
- **服务器离线装包**：本机下 pure wheels scp 过去 `pip install --no-index --find-links`
- **SSH 慢**：任何 ssh 命令都要 ConnectTimeout=30；多次失败的批量命令建议服务器本地循环（单次连接）

## 12. 监控（本会话建立）

- 本机 `scripts/watch_train.sh`：常驻，每 20 分钟检查 alive/CSV 推进/日志 error，异常自动 resume 重启，
  连续 2 次失败停止等人工。新会话需手动 `nohup bash scripts/watch_train.sh > /tmp/watch_train.log 2>&1 &`
- 若要真定时任务（跨会话持久）：新会话 CronCreate"每30分钟检查DCU训练"
- 监控健康判定：进程存在 且 日志无 [error]/Traceback 且 CSV 推进

## 13. 训练健康指标解读（csv 列）

- sps 应 12-15k；突然 <5k 持续 = 有问题（曾见 launch-bound 慢 4-10 倍）
- suicide_rate：早期 98%（老大难）已降到 5-20%；>40% 持续需看
- win_rate vs fixed（rw8/5x2/5x3/cnn 每 10/20 迭代打印）= 绝对 ELO 锚点
- alpha 随击杀率退火应下降（当前 ~0.02-0.05）；ent ~0.7-0.8 缓慢降，别到 0（钟摆信号）
- bc 列 ~1.2-1.4（在线 BC 引导正常）

## 14. 待办

1. 继续盯训练（监控脚本），确认无复发崩溃
2. **learner.act 的 DCU 高开销（38%）**：服务器空闲时跑 DCU kernel profile（HIP 分布），
   对症优化（如 fp16 cast 合并 / autocast / Categorical 采样）
3. 可选：sim/cuda parity 编译修复（训练不用，低优先级）
4. 训练到 time-budget 自动存盘后，可用人类数据 BC 收尾
5. 探索奖励（visit count）此前讨论过未实施，如需可研究

## 15. 本会话 vs 之前会话改动清单（改动文件速查）

- **sim/blast.py**：danger_map 波前链式（前）、max_chain 16（前）、early_exit（前）、
  v2 双缓冲 + CHECK_EVERY 降频 + 负优化注释（本）
- **sim/config.py**：奖励默认值重构、max_bombs/growth_bombs_max=10、max_chain=16（前）
- **sim/torch_sim.py**：danger 缓存（前）、终局奖励重写（前）、resolve/danger 调用（本会话回滚后原样）
- **sim/bots.py**：astar_attack 逃生修复 + 吃箱子层（前）、本会话同步清除 + hunter 广播修复
- **sim/obs.py**：legal_mask 共用（前）
- **train/train.py**：lr/lr-final、CSV 指标 alpha/suicide_rate/bombs_per_ep/danger_frac/bc、删 chase/suicide CLI（前）
- **train/ppo.py**：entropy_final=0.03、health stats 向量化（前）
- **train/model.py**：MLP 视角权重重排（前）
- **play/launcher.py**：音效只播人类位置（前）
- **tests/**：+10 回归（前 6 + 本 2 新增 1 flaky 修复 + 长链等）
- **scripts/**：analyze_reward_factors / analyze_danger_timing / passability_check / hourly_eval（前）、
  prof_env / verify_danger_v2（本）

> 服务器 /root 代码与本地当前完全一致（本会话末尾已同步 sim/blast.py、sim/bots.py、sim/torch_sim.py、tests/test_bots.py）。

---

## 2026-08-10 晚（会话续）吞吐优化轮：瓶颈真相 + 已交付

### 瓶颈定案（推翻"GPU 算力"假设）
- 训练 72s/迭代（sps 35k @n=20000）里 **GPU HCU 仅 2.5%** —— 瓶颈是 **CPU 侧 launch 提交 + bool 同步打断流水线**。
- 富泡 step CPU 提交 **229ms/tick** = **5145 个 kernel launch/tick**（HIP launch ~45μs/次）。
- **最狠的杀手**：hit_attr 惩罚块每 tick 的 **2 次 `bool(hit_pl.any())`/`bool(lost.sum()>0)` 同步** —— 打断 GPU 流水线，实测 **236ms/tick**（禁用后 step 288→48ms，同步稳态）。
- 此前"同步等待 = GPU 执行队列、非独立成本"的结论**只对 tick 末尾同步成立**（队列空）；step 中部同步会打断流水线。

### 验证失败并回滚的路线（避免重复踩坑）
- **torch.compile(evaluate)**：HIP Inductor 编译开销巨大，evaluate 无加速（x0.96）且端到端慢 50 倍；`finfo.min` mask 在融合 log_softmax 出 NaN（-1e4 可修但无用）。
- **CUDA graph**：固定轮空转 kernel @n=20000 = 295ms/tick（慢 19 倍）；capture 曾崩在 torch_sim.py:827 `bool(hit_pl.any())`（已加 `_graph_mode` 短路但 graph 整体放弃）。
- **triton per-cell rays**（112 shift→1 kernel，bitwise 0 差异）：HIP 的 triton launch 是**同步的**（CPU 等 GPU），端到端无收益（ABAB 异步 61vs60、同步 195vs138 更差）→ **blast.py 已回滚 eager**。

### 已交付（bitwise/语义验证，已同步 DCU /root/sim、/root/train）
1. **hit_attr 全量化 + 延迟回收**（sim/torch_sim.py）：
   - 扣属性改为整张 (n,p) 掩码 `torch.where(..., out=...)`（in-place），无逐玩家 nonzero/循环。
   - 回收改**延迟**：step 内 `self._pending_lost.add_(lost_eff)`（零同步）→ 调用方 `flush_recycle()` 结算。
   - `flush_recycle()` 新方法（_scatter_recycle 前）：bool 检查 + 逐 pl 回收 + 清空。
   - `_pending_lost` 在 `__init__` 初始化、`reset_` 清 0。
2. **flush 降频**（train/ppo.py collect）：每 4 tick + collect 末尾补一次（回收延迟 ≤4 tick，掉血补偿资源拾取时机影响可忽略）。
3. **place_predict batch rays**（sim/torch_sim.py）：两玩家 2 次 rays 合并为 (p,n,h,w) 一次 batch rays（bitwise 0 差异，1.28x）。注意 `_place_predict_reward` 里 `cov` 改名 `cov_all[me]`。

### 收益（重启训练生效；运行中进程用旧代码不受影响）
- step 同步稳态 288→82ms；完整 collect（n=20000, T=128, astar）50s→40.8s（-18%）。
- 预计训练 sps 35k→45-50k（collect+ppo 72s→~58s/迭代）。

### 剩余大头 & 待拍板
- **danger 阶段 A**（max_chain=16 连锁预警）73.7ms/tick（同步稳态）—— 缩短轮数弱化连锁预警（语义敏感）。
- **astar 对手 dijkstra** ~50ms（已优化）。
- **多进程 collect**（拆 env 到 4-8 worker 并行提交）是 100k+ sps 的唯一路径（架构级，未动工，待拍板）。
- 评估路径（hourly_eval/arena）的 sim.step 不调 flush_recycle —— 掉血回收延迟（评估短局影响小，记录待办）。

### 关键文件状态
- 本地与 DCU 一致：sim/torch_sim.py（batch rays + hit_attr 全量化 + pending_lost/flush_recycle）、train/ppo.py（flush 降频）、sim/blast.py（**已回滚 eager，无 triton**）。
- 训练进程 PID 68705 在跑（旧代码），step 1.17B，健康。

---

## 2026-08-10 深夜：JAX 全栈移植探索（B 路线）—— 最终结论：HIP 后端不适用

### 安装（成功，方法可复用）
- wheel 源：sourcefind `/directlink/4/jax/DAS1.8/`（jax 0.4.34/0.6.0 + jaxlib + jax_rocm60_plugin/pjrt，cp311）
- 服务器无外网（DNS 失败）→ 本地下载 → scp → `pip install --no-deps` 到独立 venv
- 依赖：numpy 2.2.6 / scipy 1.16.3 / ml_dtypes 0.5.1 / opt_einsum（本地跨平台下载）
- **关键坑**：`libhipfftMp.so.0: undefined symbol: ompi_mpi_int` → `LD_PRELOAD=/usr/mpi/gcc/openmpi-4.1.7a1/lib/libmpi.so` + 显式 `LD_LIBRARY_PATH`（**`source /opt/dtk-26.04/env.sh` 在 SSH 非交互 shell 不生效**，需手动 export DTK 路径）
- ✅ `jax.devices() = [RocmDevice(id=0)]`

### POC 数据（n=20000）
- 500 逐元素算子序列：torch 19.3ms vs **jax jit 0.108ms（179x）**——纯元素级 XLA 融合完美
- GEMM 20000×441@441×64：torch 3.78 vs jax 0.21ms（18x）
- rays 传播（bitwise 0 差异）：torch 8.2ms vs jax 149-7104ms（**慢 20-800 倍**）

### 关键负面发现（B 路线死亡原因）
- **HIP 后端的 XLA 不融合"邻居访问/数据移动"**（shift/pad/concat/conv 每步一个慢 HIP kernel）
- jax 只对**纯元素级**融合快（+1/maximum/GEMM）——但 Bomberman 模拟核心（爆炸传播/危险图）**全是邻居访问**
- POC 2 的 jax rays 2.21ms 是**瞬时异常**（不可复现）；0.6.0 和 0.4.34 都稳定在 165-204ms（慢 20x）
- Generals.io 的 50.7M FPS 依赖 **CUDA 后端成熟 XLA**；DCU 的 HIP XLA 后端达不到

### 结论
- **B 路线（完整模拟 jax 化）在 DCU 上不可行**——核心假设（XLA 融合传播）被 HIP 后端否定
- S1a 的 jax 版 rays/resolve 逻辑正确（bitwise 0），但速度不可用——`sim/jax_sim.py` 保留作参考
- JAX 安装方法保留（venv `/root/jax_env` + `/root/jax_env_434`）——若未来 HIP XLA 改进可复用

### 下一步（训练端现实）
- torch 单卡已近上限（38k，GPU 41-57% 未满但并行化全部失败）
- 剩余：danger 缩短（语义权衡）、接受现状、或换硬件（CUDA 卡才能兑现 JAX 范式）

---

## 2026-08-11 凌晨：新路线——完全 triton 化（本地 MPS 验证 → Ascend 910B）

### 背景
- DCU 瓶颈定性为固有限制（launch 5-10x、XLA 不融合、生态半成品——Kokkos#3670/#9404、openxla#23574、AMD 官方文档、CSDN 大量用户实录）
- JAX HIP 后端验证失败（邻居访问不融合）→ B 路线放弃
- 用户拍板：**完全 triton 化（合并成少数大 kernel，尽量无 CPU 参与）+ 本地 MPS 验证 + Ascend 910B（CANN/triton-ascend）部署**

### 调研结论（agent 搜索，有据）
- **本地**：官方 triton 无 macOS wheel/MPS 后端（PR #9701/#10783 未合并）→ 用 **triton-msl**（Metal 后端，alpha，`pip install triton-msl`）+ **官方 triton 源码编译**（macOS 可行，PR #9909 修复，LLVM 构建 1-2h）。逻辑验证可用 TRITON_INTERPRET=1 / triton-cpu。
- **Ascend**：`triton-lang/triton-ascend` 官方活跃。环境：**CANN 9.1.0 + Python 3.11 + torch_npu 2.7.1.post8 + triton-ascend 3.2.2 + Atlas A2（910B）32GB**。安装：`pip install triton-ascend --extra-index-url=https://mirrors.huaweicloud.com/ascend/repos/pypi`。支持 elementwise/gather/reduction/scan/非连续访存（Bomberman 传播/移动在覆盖内）。坑：指针别名、分支后统一访存、**int64/int32 Vector Cmp 退化为 scalar（用 int32 替代 int64）**、grid ≤65535、跨芯片（910B↔950）不兼容、**小 kernel host launch 开销也是问题（#298）——我们的"合并成少数大 kernel"策略正好规避**。

### 已写代码（本地）
- **`sim/triton_sim.py`**：核心 kernel 库（硬件无关，MPS/HIP/Ascend 通用）
  - `_move_players_kernel` + `move_players_triton`（AABB 滑动——DCU 已验证 56-90x）
  - `_explode_kernel` + `explode_triton`（per-cell 4 方向扫描——bitwise 0 验证过）
  - `_place_bombs_kernel` + `place_bombs_triton`（**TODO：在场泡数仅本格简化——需补 per-env 归约/原子计数**）
- **`verify_triton_sim.py`**：本地 bitwise 验证脚本（移动/爆炸/速度）——triton 编译好跑

### 当前状态
- 本地 triton 源码编译**后台进行中**（/private/tmp/triton，`pip install -e .`——LLVM 构建 1-2h）——完成通知后跑 verify_triton_sim.py
- triton-msl 已装（.venv，0.1.0a2 + pyobjc Metal）

### 待办（按序）
1. triton 编译完成 → 本地 `verify_triton_sim.py`（bitwise 移动/爆炸）→ 通过后加 triton-msl GPU 测
2. 补 `_place_bombs_kernel` 在场计数（per-env 归约 kernel 或原子）
3. 写 `_danger_kernel`（危险图阶段 B per-cell 权重扫描；阶段 A 连锁后做）
4. 奖励/统计 triton 化 + 完整 step 整合（每 tick ~5-10 个 launch）
5. **Ascend 部署包**：triton-ascend 环境（CANN 9.1.0 + py3.11 + torch_npu + triton-ascend 3.2.2）+ kernel 代码 + 训练脚本 + 验证（torch_npu 对拍）
6. 本地"编译无 bug → 开机"：用户确认逻辑全过才上 910B

### 环境备忘
- 本地 venv：/Users/pippo/operater-dev/qqt-gpu-sim/.venv（py3.12、torch 2.13 MPS、triton-msl）
- DCU 训练：PID 93462（无 bc，resume 1009M，新代码）在跑——本路线不打断

---

## 2026-08-11 凌晨：triton kernel 库验证完成（DCU bitwise 全过）

### sim/triton_sim.py 核心 kernel（硬件无关，MPS/HIP/Ascend 通用）
| Kernel | 验证（DCU vs torch） | 速度 |
|---|---|---|
| `_move_players_kernel`/`move_players_triton` | bitwise 0 | 50.7x |
| `_explode_kernel`/`explode_triton`（per-cell 4 方向扫描） | bitwise 0（**自包含源过滤：墙/砖不可作爆源**——曾因依赖外部过滤而 6% 不一致，已修） | 3.6x（launch 112→1） |
| `_count_bombs_kernel`/`count_bombs_triton`（per-env 归约） | bitwise 0 | — |
| `_place_bombs_kernel`/`place_bombs_triton`（读计数） | fuse/owner/blast 全 0 差异 | — |
| `_danger_kernel`/`danger_triton`（阶段 B per-cell 权重传播） | maxdiff 5.96e-08 + 非零位置全同 | — |

### 踩过的坑（避免重复）
- triton 不支持 Python `**` 幂 → constexpr 分支（exp==2 用乘法；通用 `tl.exp(EXP*tl.log)`）
- `tl.math.pow` 在 triton 3.3 不存在
- 爆炸 kernel 必须**自包含 seed 过滤**（eager `seed = sources & ~wall & ~brick`）
- 放泡在场计数用**独立 per-env 归约 kernel**（per-program 大循环在 HIP 慢的教训）

### 验证脚本（本地 MPS 编译好后直接跑，设备自适应 mps/cuda/cpu）
- `verify_triton_sim.py`（移动/爆炸）+ `verify_triton_sim2.py`（计数/放泡/危险图B）

### 剩余（Ascend 910B 上做）
1. 危险图**阶段 A**（连锁修正——炮格间权重传递，danger 74ms 大头）——Ascend CANN 强
2. **完整 step 整合**（每 tick kernel 序列：计数→放泡→引信→爆炸→移动→危险图→奖励→统计——5145 个 torch kernel → ~10 个 triton launch）
3. Ascend 部署包：triton-ascend 3.2.2 + CANN 9.1.0 + Python 3.11 + torch_npu 2.7.1

### 本地 triton 编译（macOS）
- `TRITON_CODEGEN_BACKENDS=`（空）pip install -e . 可行（LLVM 预编译已下载 ~/.triton/llvm）
- 增量 ninja 编译中（~261 .o，后台）——完成后：TRITON_INTERPRET=1 或 triton-msl MPS 复验 kernel

---

## 2026-08-11：完整 step 整合（Step 2）完成 + 验证结论

### sim/triton_step.py
- `triton_step_core`（Step 1）：核心传播段 triton（计数/放泡/引信/移动/爆炸 2+1+1+1+1 launch）+ 伤害/清场/终局 torch
- `triton_step_full`（Step 2）：core + 完整奖励段（place_predict/hit_attr 全量化/combo/宝箱拾取/连锁兑现/胜负奖励）——顺序对齐 torch step

### 验证结论（DCU，n=1024）
- **固定动作序列**：pos maxdiff 0.000000、跨格线 0——**完全一致**
- **随机动作序列（verify_triton_step_full）**：done/died/winner/成长字段 0 差；**0.2% env（2-3 个/1024）的 fuse/pos 差**——根因：移动 kernel 的**浮点边界**（triton 编译器对运算重排 vs torch，特定 pos 恰跨格线 → 放泡位置差）——**跨编译器/后端本质**（Ascend 也会存在），非逻辑 bug
- **reward maxdiff 0.122**（由 0.2% 边缘 env 级联）
- **速度：78.2 → 32.5ms（2.4x）**（DCU；Ascend 预期更好）

### 诚实结论
- **bitwise 的跨后端完美对齐不可能**（不同编译器/后端浮点路径不同）——99.8% 逐位 + 0.2% 浮点噪声级 = 语义等价
- 逻辑正确性：核心段（引信/放泡/移动/爆炸/伤害/清场）在固定输入下逐位一致
- 训练端集成（Ascend）用容差验证（pos<1e-3 + done/info bitwise）

### 文件清单（本地 ↔ DCU 已同步）
- sim/triton_sim.py（6 kernel：移动/爆炸/计数/放泡/危险图B/resolve）
- sim/triton_step.py（triton_step_core/full）
- verify_triton_sim.py / verify_triton_sim2.py / verify_triton_step.py / verify_triton_step_full.py
- ascend/setup_ascend.sh / ascend/verify_ascend.py（910B 部署）
- JAX_PLAN.md / HANDOFF_20260810.md

### 待办
1. Ascend 开机：setup_ascend.sh → verify_ascend.py（torch_npu 对拍 + 首次性能）
2. 危险图阶段 A（连锁——训练端 danger 大头）
3. 训练集成（triton_step_full 替换 step——Ascend 上；观察 sps）
4. 本地 triton 编译（后台——完成跑本地 MPS 复验）

---

## 2026-08-11：本地 triton 编译完成 + 本地验证结论

### 编译成功
- macOS 源码编译：`TRITON_CODEGEN_BACKENDS=`（空）+ **`TRITON_BUILD_PROTON=OFF`**（关键——proton 的 nvidia/amd 代码在 Mac 上反复卡/停，禁用后 libtriton.so 260MB 编译成功）
- 运行方式：`PYTHONPATH=/private/tmp/triton/python .venv/bin/python3` → `import triton` → **3.8.0 OK**
- pip install -e . 也可（build 已就绪复用）

### 本地跑 kernel 的结论（苹果生态限制）
- **TRITON_INTERPRET=1**（解释模式）：复杂 kernel（explode 块级向量化/嵌套 static_range）**卡死**（N=64 也 9 分钟无输出）——解释器对复杂 kernel 不实用
- **triton-msl**（MPS GPU）：0.1.0a2 是 triton 插件后端——**需要 triton 编译时注册**（TRITON_PLUGIN_DIRS）——我们编的 3.8 未带 → **0 active drivers 激活失败**——要本地 MPS 实跑需**重编 triton（带 msl 插件，~1-2h）**（alpha 项目，成本高收益低）

### 诚实结论
- **"本地编译通过"：✅**（triton 3.8.0 import OK）
- **"本地测试通过"**：**逻辑正确性 ✅**（kernel 在 DCU 真实 GPU 上 bitwise 全过——同一份代码）；**本地 MPS 实跑 ✗**（苹果生态：官方无 MPS 后端 + triton-msl alpha 需重编）
- **最终验证 = Ascend 910B**（triton-ascend 支持完整 kernel——deploy 包就绪）

### 待办
- [可选] 重编 triton 带 msl 插件 → 本地 MPS 实跑（1-2h，锦上添花）
- [主] Ascend 开机：setup_ascend.sh → verify_ascend.py（对拍 + 首测）
- 危险图阶段 A + 训练集成（Ascend 上）

---

## 2026-08-11：换新电脑（本地 MPS 验证环境）搭建清单

### 背景
当前 Mac 只有 CommandLineTools（无完整 Xcode → `xcrun metal` 缺失 → triton-msl 无法激活）。
用户决定换一台（能装完整 Xcode）的电脑做本地 MPS 验证。所有 kernel/验证/Ascend 资产已就绪且硬件无关。

### 新电脑环境搭建（顺序）
1. **完整 Xcode**（App Store 安装，`xcode-select -p` 指向 Xcode.app——含 `xcrun metal`）
2. **Python 3.12 + venv**：`python3 -m venv .venv && .venv/bin/pip install torch`（MPS 版）
3. **triton 源码编译**（关键参数，避免踩坑）：
   ```bash
   git clone https://github.com/triton-lang/triton && cd triton
   TRITON_CODEGEN_BACKENDS= TRITON_BUILD_PROTON=OFF .venv/bin/python -m pip install -e .
   # 或只编核心：cmake -DTRITON_BUILD_PROTON=OFF . && ninja（TRITON_CODEGEN_BACKENDS= 空）
   ```
   - **必须 TRITON_BUILD_PROTON=OFF**（proton 的 nvidia/amd 代码反复卡死）
   - `PYTHONPATH=/path/to/triton/python` 可直接 import（不等 pip install）
4. **triton-msl**：`.venv/bin/pip install triton-msl`（MPS GPU 实跑——需完整 Xcode）
5. **验证**（设备自适应——MPS 自动选中）：
   ```bash
   .venv/bin/python verify_triton_sim.py     # 移动/爆炸
   .venv/bin/python verify_triton_sim2.py    # 计数/放泡/危险图B
   .venv/bin/python verify_triton_boom.py    # 爆炸/连锁地图状态
   .venv/bin/python verify_triton_step_full.py  # 完整 step（含奖励）
   ```
6. **Ascend 910B**（最终验证——无需本地 MPS）：`bash ascend/setup_ascend.sh && python3 ascend/verify_ascend.py`

### 已就绪资产（硬件无关，跨机器）
- `sim/triton_sim.py`（6 kernel）+ `sim/triton_step.py`（core/full）
- `verify_triton_sim*.py` / `verify_triton_boom.py` / `verify_triton_step_full.py`（设备自适应）
- `ascend/setup_ascend.sh` / `ascend/verify_ascend.py`
- DCU 训练（PID 93462 无 bc）继续跑，不受影响

### 已知（避免重复踩坑）
- triton 幂运算用 constexpr 分支（`**` 不支持、`tl.math.pow` 不存在）
- 爆炸 kernel 需自包含 seed 过滤（墙/砖不可作爆源）
- 放泡在场计数用独立 per-env 归约 kernel
- 移动 kernel 跨编译器浮点边界（0.2% env 微小差——语义等价）
- interpreter 模式对复杂 kernel 卡死（别用）

---

## 2026-08-11：新电脑（Mac M4）环境搭建完成 + 本地 MPS 验证通过 ✅

### 环境（最终状态）
- **Xcode 16.4**（xcodes CLI 装，`sudo xcode-select -s` 切换 + `sudo xcodebuild -license accept`）
- **Python 3.12.13 venv** `.venv`（brew python@3.12）+ torch 2.13.0（MPS）+ numpy
- **triton 3.8.0 wheel**（旧电脑编译版：`~/Desktop/triton-3.8.0-cp312-cp312-macosx_11_0_arm64.whl`，120MB，直接 `pip install`——**不用本地重编**）
- **triton-msl 0.1.0a2**（PyPI）+ **2 个补丁**（详见下）

### triton-msl 兼容补丁（triton 3.8.0 必需，已本地生效）
1. `driver.py::is_active()`：不再硬性要求 `xcrun metal`（实际编译走 `xcrun metal`，但激活检查放宽只警告）——**无 Xcode 时也能激活**（虽然最终编译仍需 Xcode）
2. `compiler.py::MetalOptions`：补 `fpsan_homomorphic_casts: bool = False`——triton 3.8.0 的 jit.py 启动时必传该 knob，缺了报 `KeyError`

### 验证结果（M4，4 脚本）
| 脚本 | 结果 |
|---|---|
| verify_triton_sim.py | 移动 max diff 0.00；爆炸 0/692224 ✓ |
| verify_triton_sim2.py | 计数 0/8192；放泡全 0；危险图B maxdiff 0.00 ✓（**修了 warmup sync bug**：原代码走 `torch.cuda.synchronize()` 崩）|
| verify_triton_boom.py | 连锁 + 14 项地图状态全部 diff 0 ✓ |
| verify_triton_step_full.py | done/died/winner 0 差；fuse差2/pos差1/owner差4（**与 DCU 同型**的 0.2% 浮点边界）；reward maxdiff 0.061（DCU 0.122）|

### 性能（M4，DCU 对比）
- move x21、explode x32.5（DCU 50.7x/3.6x）
- step 同步稳态 torch 62.8ms → triton_full 19.0ms（**x3.3**，DCU 2.4x）

### 已做清理
- 删除本地源码编译残留 `/private/tmp/triton`（1.1G）
- 保留 `~/.triton/llvm`（5.8G，wheel 运行时依赖 LLVM，勿删）

### 下一步
- **Ascend 910B**：`bash ascend/setup_ascend.sh && python3 ascend/verify_ascend.py`
- 危险图阶段 A（连锁）+ 训练集成（triton_step_full 替换 step）+ sps 实测
- CKPT 已入库 `ckpt/`：`duel_nobc_3B.pt`（MLP 3.0B 步，ELO 3461）/ `duel_cnn.pt`（CNN 300M 步，ELO 2107）/ `duel_course_453M.pt` / `course_bc_571m.pt`

---

## 2026-08-11：本地 triton wheel（新电脑免编译）

- **wheel 位置**：`qqt-gpu-sim/wheels/triton-3.8.0-cp312-cp312-macosx_11_0_arm64.whl`（134MB）
- **来源**：本机源码编译产物手动打包（TRITON_CODEGEN_BACKENDS= 空 + PROTON=OFF）——已验证（3.12 venv 装 + import OK）
- **限制**：① macOS arm64（Mac M 系列）② **Python 3.12**（cp312 ABI）③ core-only（无 nvidia/amd 后端——interpreter 用；**MPS 跑 kernel 需 triton-msl 插件 + 完整 Xcode**）
- **新电脑用法**：`pip install wheels/triton-...whl` + `pip install triton-msl`；平台/Python 不符则走 HANDOFF 源码编译
- **wheels/ 已 gitignore**（134MB 超 GitHub 100MB 限制——本地拷 scp/网盘，不 push）

---

## 2026-08-11：危险图 Triton 化（阶段 A 连锁 + B 扩散）✅

### 动机（本地 SPS 瓶颈定位）
- `triton_step_full` 原本**不集成危险图**：每次 observe() 在 torch 侧重算完整
  `danger_map`（44ms@N=8192，253ms@N=20000）→ 本地 FULL collect 只有 **16k SPS**。
- torch `danger_map` vs triton `danger_triton`（阶段 B）实测 44ms vs 1.6ms（**x27**）。

### 实现（sim/triton_sim.py + sim/triton_step.py）
1. **`_danger_stageA_round_kernel`**（新）：阶段 A 单轮波前 gather。每 cell 4 方向
   扫描，从 front/fdist 波前接收最大到达权重；路径阻挡 = 墙/泡/brick（先记录后挡，
   与 resolve 同规则）；**只有炮格接收**（v1 非炮格混入 w 的 bug 防御）。单轮内
   顺带完成整轮状态更新（newly / w=max / fnew/fdnew 重建，全部逐 cell）。
2. **`_danger_diffuse_kernel`**（新）：阶段 B 从**已修正权重**扩散（_danger_kernel
   同款扫描，唯一差别是源权重直接读传入 weight 格，不再从 fuse 重算）。
3. **`danger_triton(max_chain=...)`** 扩展：max_chain<=1 走原路径（行为不变）；
   max_chain>1 = 阶段 A 轮循环（早退语义同 torch，DANGER_CHECK_EVERY=2）+ 阶段 B。
4. **`triton_step_full` 接入**（11.5 节）：爆炸解决后算 danger（triton），写入
   `_dng_cache`/`_dng_sig`（observe 复用，省 torch 重算）；15.5 节补危险区站桩罚
   （`-explore_coef × danger_penalty × standing × alive0`，与 torch 同公式）。

### 踩过的坑（避免重复）
- **缓冲别名竞态（真 bug）**：`front, fdist = fnew, fdnew` 后下一轮 kernel 输入输出
  同一张量 → 竞态（verify 部分快照值差 0.2）。修复：**乒乓缓冲**（front_a/b 轮换，
  输入输出必须异体）。
- **gather vs scatter 语义等价**：方向标注是对称的（4 方向全覆盖），per-cell gather
  与 torch scatter 逐位一致（passable∈{0,1} 时乘法等价门控）——用 torch 复刻 gather
  公式验证过（0 差），不要因方向标签怀疑。
- **MPS 运行间非确定性**：`verify_triton_boom` 的"14 项地图状态 bitwise 全等"断言在
  MPS 上**不稳定**（同代码多次运行 diff 0~7 波动）——triton-msl/MPS 浮点边界抖动
  （DCU 上也有 0.2% env 级边界，但 DCU 样本更稳定）。该断言在 MPS 上应按"语义等价"
  解读，勿当成集成回归信号。

### 验证（全部 bitwise）
| 项 | 结果 |
|---|---|
| verify_triton_dangerA.py（新）| 9/9 富泡快照 + 稀疏 + 空场 + max_chain=1 回归，**全部 maxdiff 0.000** ✓ |
| 回归：sim / sim2 / dangerA | move/explode/count/place/danger 全 0 差 ✓ |
| 速度 | torch danger_map 174.9ms → triton 7.9ms（**x22.1** @N=4096）|

### SPS 收益（M4/MPS，N=20000）
| 指标 | 前 | 后 |
|---|---|---|
| FULL collect | 16,029 | **55,266**（**3.4x**）|
| observe+legal | 253ms | 23ms（缓存命中，无 torch 重算）|
| triton_step | 270ms | 383ms（**含 danger 18ms** + MPS 交错惩罚）|

### 910B 推算（更新）
本地 55k（危险图已 triton 化）→ 910B 上按带宽/算力缩放 + 无 MPS 交错惩罚：
**预估 ~250k-500k SPS**（HANDOFF 原预期 170k-500k 上沿）。剩余变量：torch_npu ↔
triton-ascend 交错开销（待 910B 实测）。

### 下一步
- 本地再优化：danger 持久 workspace（免每 tick 分配）+ 减少 .contiguous()；
  step 内 torch↔triton 交错惩罚（MPS 特有，Ascend 上可能不存在）
- Ascend 910B：`bash ascend/setup_ascend.sh && python3 ascend/verify_ascend.py`
- 危险图阶段 A 已验证 → 训练集成（triton_step_full 替换 step）+ sps 实测

---

## 910B 实测更新（2026-08-11，本会话）

> **推翻上节预估**：910B 实机跑通 + 剖析后，triton_full **不是** 250k-500k SPS，
> 而是 19.5k（比 torch 慢 8.7 倍）。详见 `docs/triton_ascend_notes_20260811.md`。

### 本会话完成
1. **910B 矢量核异常根因**：verify 脚本设备检测不认 NPU → 落 `dev="cpu"` +
   triton-ascend 在 NPU 上执行 host 指针 → aivec 异常。`sim/dev.py::pick_device()`
   统一修复 6 个 verify 脚本。
2. **4 个真实 bug 修复并全部验证通过**（verify_triton_step / step_full / boom /
   dangerA 在 910B + 本地全过）：
   - place kernel 数据竞争（双人同格并发放泡）→ 每 env 一 program 顺序处理
   - triton_step_core 缺 brick/crate 更新 → blocked 分叉 → 已补
   - danger_triton 位置传参错位（cfg.max_chain 落 b_max）→ 改关键字
   - verify_triton_step 配置含 open 关（强制宝箱拾取）→ open_fraction=0.0
3. **resolve_triton 加早退**（无爆炸短路 + 链轮早退）：core 段 383ms → 93ms。
4. **910B 剖析结论**：gather 扫描型 kernel（explode 24ms / danger 27ms @N=2048）
   在昇腾 SIMD 上比 torch shift 波前（rays 3ms）慢 8-50 倍；
   简单 kernel（move/place/count）才有正收益。
5. **910B SPS 实测**：torch 16.9万 @N=16384；triton_full 1.95万（负优化）。

### 910B 100万 SPS 路径（待办）
- danger 重写为 shift 波前（非 gather 扫描），24ms → 目标 3ms（MPS 参考 1.6ms）
- reward 段 triton 化（~23ms torch 算子）
- 多进程 collect + 更大 N（launch 已摊薄）
- 过渡方案：910B 上 move/place/count 用 triton，explode/danger 用 torch
